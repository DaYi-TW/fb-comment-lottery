## MODIFIED Requirements
### Requirement: Web Interface
The system SHALL provide a **React-based** web interface for scraping and running lotteries.

#### Scenario: Reactive State
- **WHEN** the scraping status updates (loading -> done)
- **THEN** the UI updates immediately without a page reload
